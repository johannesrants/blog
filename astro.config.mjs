import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://taupe-cannoli-0f6795.netlify.app', // Update with your Netlify domain or custom domain
  // base: '/blog', // Only needed for GitHub Pages subdirectory deployment
  integrations: [],
});

